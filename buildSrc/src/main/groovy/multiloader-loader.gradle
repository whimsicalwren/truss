plugins {
    id 'multiloader-common'
}

configurations {
    coreJava {
        canBeResolved = true
    }
    coreKotlin {
        canBeResolved = true
    }
    coreResources {
        canBeResolved = true
    }
    commonJava{
        canBeResolved = true
    }
    commonKotlin {
        canBeResolved = true
    }
    commonResources{
        canBeResolved = true
    }
}

dependencies {
    compileOnly(project(':core'))
    compileOnly(project(':common')) {
        capabilities {
            requireCapability "$group:$mod_id"
        }
    }

    coreJava project(path: ':core', configuration: 'coreJava')
    coreKotlin project(path: ':core', configuration: 'coreKotlin')
    coreResources project(path: ':core', configuration: 'coreResources')
    commonJava project(path: ':common', configuration: 'commonJava')
    commonKotlin project(path: ':common', configuration: 'commonKotlin')
    commonResources project(path: ':common', configuration: 'commonResources')
}

tasks.named('compileJava', JavaCompile) {
    dependsOn(configurations.coreJava, configurations.commonJava)
    source(configurations.coreJava, configurations.commonJava)
}

tasks.named('compileKotlin') {
    dependsOn(configurations.coreKotlin, configurations.commonKotlin)
    source(configurations.coreKotlin, configurations.commonKotlin)
}

processResources {
    dependsOn(configurations.coreResources, configurations.commonResources)
    from(configurations.coreResources, configurations.commonResources)
}

tasks.named('javadoc', Javadoc).configure {
    dependsOn(configurations.coreJava, configurations.commonJava)
    source(configurations.coreJava, configurations.commonJava)
}

tasks.named('sourcesJar', Jar) {
    dependsOn(configurations.coreJava, configurations.commonJava)
    from(configurations.coreJava, configurations.commonJava)
    dependsOn(configurations.coreResources, configurations.commonResources)
    from(configurations.coreResources, configurations.commonResources)
}